# RetiroConecta - versión final ajustada

Cambios realizados para cubrir la consigna de forma más literal:

1. Se agregó `consulta-viajes.html` como página propia de consulta de viajes.
2. Se agregó el enlace "Consulta de viajes" al menú principal y al footer de todas las páginas.
3. Se mantuvo el simulador existente con datos simulados mediante JavaScript.
4. Se unificó la Ruta 2 con el enlace correcto: https://martinagiu2014-cyber.github.io/PAGINA-WEB-DE-TRANSPORTE/
5. Se ajustó el CSS para que el menú con la nueva sección siga funcionando en escritorio y mobile.
6. Se dejó el JS más defensivo para evitar errores si una página no tiene todos los elementos del simulador.

Para publicar: subir todos estos archivos manteniendo la estructura y la carpeta `assets`.
